spring:
    profiles:
        actice:
        - dev
---
server:
    port: 8201
spring:
    profiles: dev
    application:
        name: configClient
eureka:
    client:
        service-url:
            defaultZone: http://localhost:7001/eureka/